@model IEnumerable<Doanphanmem.Models.KhachHang>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

 


<div class="page-header">
    <div class="header-sub-title">
        <nav class="breadcrumb breadcrumb-dash">
            <a href="~/Admin/" class="breadcrumb-item"><i class="anticon anticon-home m-r-5"></i>Dashboard</a>
            <span class="breadcrumb-item active">Quản lý tài khoản người dùng</span>
        </nav>
    </div>
</div>

<div class="card">
  
    <div class="card-body">
        <h2 class="header-title" style="text-align:center;">Quản lý tài khoản người dùng</h2>
        <form class="d-flex" role="search" style="margin-bottom:40px;">
            <input class="form-control me-2" type="text" name="SearchString" placeholder="Search" aria-label="Nhập từ khoá ">
            <button class="btn btn-outline-success" type="submit">Search</button>
        </form>
        <div class="row m-b-30">
            <div class="col-lg-12 text-right">
                <a href="/KhachHangAdmin/Create" class="btn btn-primary">
                    <i class="anticon anticon-plus-circle m-r-5"></i>
                    <span>Thêm mới</span>
                </a>
            </div>
        </div>

        <div class="table-responsive">
            <table class="table table-hover e-commerce-table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Tên</th>
                        <th>SĐT</th>
                        <th>Email</th>
                        <th>Địa Chỉ</th>
                        <th>Ngày Sinh</th>
                        <th>Tài Khoản</th>
                        <th>Mật Khẩu</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                #@item.MaKH
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.TenKH)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.sdt)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.email)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.DiaChi)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.NgaySinh)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.TK)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Pass)
                            </td>
                            <td class="text-right">
                                <a href="/KhachHangAdmin/Edit/@item.MaKH" class="btn btn-icon btn-hover btn-sm btn-rounded pull-right">
                                    <i class="anticon anticon-edit"></i>
                                </a>
                                <a href="/KhachHangAdmin/Delete/@item.MaKH" class="btn btn-icon btn-hover btn-sm btn-rounded">
                                    <i class="anticon anticon-delete"></i>
                                </a>
                            </td>
                        </tr>

                    }
                </tbody>
            </table>
        </div>
    </div>
</div>